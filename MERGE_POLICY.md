# 경제한입 자동화 머지 정책 (MERGE_POLICY)

키리(채널 봇)가 econ-bite 자동 작업을 할 때 **무엇을 알아서 머지하고 무엇을 대표님께 물어볼지** 정하는 기준.
대표님 승인: 2026-06-15. 가이드 바꾸려면 이 파일만 고치면 됨.

원칙: **안전한 건 알아서 머지, 큰 건 같이 정하고, 애매하면 무조건 물어보기.**
모든 판단의 기본값은 **"애매하면 리뷰行(머지 X)"**.

---

## ✅ 자동 머지 OK (빌드 통과 시 바로 `gh pr merge --squash --delete-branch`)

서스테이닝·소규모 기능개선 범주:

- **SEO / 노출**: 메타 설명, JSON-LD, sitemap, robots, OG 태그, 내부 링크, 관련글, 카테고리/태그 페이지
- **접근성**: aria, 색 대비, 시맨틱 마크업, 키보드 내비
- **콘텐츠 다듬기**: 카피 개선, 오타, 내부 링크 보강
- **작은 UI/스타일**: 레이아웃 안 깨지는 선의 디자인 정리·간격·반응형 소소한 수정
- **성능 소소**: 이미지/폰트 최적화, lazy load, 번들 소소한 정리
- **유지보수**: 의존성 **패치** 업데이트, 린트/포맷, 죽은 코드 제거, 주석

### 새 글 발행 (auto-post) — 무조건 자동 발행
- 새 경제 글은 **리뷰 없이 무조건 자동 머지**. (대표님 승인 2026-06-15)
- 빌드만 통과하면 바로 머지·발행. 빌드 실패하면 고치고, 못 고치면 PR 열어둔 채 보고.

**자동 머지 공통 조건**: `npm run build` 통과 + 기존 페이지 안 깨짐 + 변경 범위 작음.
하나라도 불충족 → 리뷰行.

---

## 🙋 리뷰 필요 (머지 X, PR 열어두고 대표님 DM 보고)

크리티컬·구조적·취향 영역:

- **리팩토링 / 구조 변경**: 디렉토리 구조, 파일 대거 이동·이름변경, 추상화 변경
- **새 기능 추가**: 새 라우트/페이지 타입, 데이터 모델·스키마 변경, 외부 API 연동
- **디자인·브랜드**: 톤/색/타이포 등 사이트 인상 바뀌는 변화
- **인프라/빌드/배포**: 빌드 설정, CI, 호스팅, env, 의존성 **major** 업데이트
- **수익화**: 광고·애드센스·결제·제휴 코드
- **그 외 조금이라도 애매하면 전부 리뷰行** (기본값)

리뷰 보고 형식 (DM):
`🙋 이건 봐줘: <제목>\n이유: <왜 리뷰 필요한지>\n👉 <PR URL>`

---

## 작업 흐름 요약

1. `cd ~/projects/econ-bite && git checkout main && git pull`
2. 작업 1건 구현 + `npm run build` 통과 확인
3. `auto/<post|feat>-<slug>` 브랜치 커밋·push, `gh pr create` (base main)
4. 이 정책으로 판단 → 자동머지 or 리뷰行
5. 결과 대표님 DM 보고 (자동머지: 한 줄 / 리뷰행: 위 형식 / 할 것 없으면 조용히 패스)

---

## ⚠️ 머지가 막혔을 때 — 에러 문구로 원인을 정하지 않는다

`GraphQL: Pull Request is not mergeable` 은 **충돌이라는 뜻이 아니다.** 체크가 아직 도는 중이어도 같은 문구가 난다.

```
확인 순서   gh pr view <번호> --json mergeable,mergeStateStatus
            mergeable=MERGEABLE · status=UNSTABLE   → 판정 대기. 잠시 뒤 재시도
            mergeable=CONFLICTING                    → 그때가 진짜 충돌
```

🔴 **뒤처졌다고 지레짐작해 리베이스부터 하지 않는다.** 포함 여부는 `git merge-base --is-ancestor origin/main HEAD` 로 확인한다.

> 2026-09-06 시황 PR #332: "not mergeable" 을 보고 그 사이 머지된 다른 PR 때문이라 판단해 리베이스했는데,
> `merge-base` 로 보니 **이미 포함돼 있었다.** 원인은 체크 진행 중이었고 재시도로 그냥 머지됐다. (꼬미)
