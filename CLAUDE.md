# 옳은경제 🧭 — 자동 글쓰기 가이드

옳은경제은 경제 개념을 쉽고 정확하게로 정리하는 광고 수익형 콘텐츠 사이트 (Astro). 검색 유입(SEO)이 수익의 핵심이라, 정확하고 충실한 글을 꾸준히 쌓는 게 목표.

이 파일은 **2시간마다 도는 자동 글 생성 작업**이 따르는 가이드다.

> 💡 **소재 선정·문체 다듬기 실전 방법론은 [`WRITING_PLAYBOOK.md`](./WRITING_PLAYBOOK.md) 참조** (경제=키리 / 부동산=복덕 공용).

## 글 작성 규칙

1. **위치**: `src/content/posts/<영문-slug>.md` (slug은 주제를 나타내는 영문 케밥케이스). ⭐ **slug은 짧게 — 핵심 키워드 3~4단어 이내**로 (예: `us-treasury-yield`, `movein-supply-2yr`). 제목을 통째로 옮긴 긴 나열(`us-long-term-treasury-yield-19yr-high` 같은) 금지 — 공유 시 URL이 지저분하고, 짧은 키워드가 SEO·클릭률에도 유리. (2026-08-18 확정, 기존 글 slug은 유지)
2. **중복 금지**: 글을 쓰기 전 `src/content/posts/` 의 기존 글 목록과 각 frontmatter title을 확인하고, 주제가 겹치지 않는 새 주제를 고른다.
3. **frontmatter** (필수):
   ```
   title: "제목 (질문형/후킹형 권장)"
   description: "한 줄 요약 — 목록 카드 + 검색결과 설명에 쓰임. 검색 의도를 담아 작성."
   category: "기초 개념"
   publishedDate: <오늘 날짜 YYYY-MM-DD>
   tags: ["키워드1", "키워드2"]
   ```
4. **카테고리**는 다음 중 하나: `기초 개념`, `지표 읽기`, `투자`, `생활 경제`, `부동산`, `경제 뉴스` (`부동산`은 복덕 담당 테마)
5. **톤**: 쉽고 명확하게. 어려운 용어는 일상 비유로 풀이. 소제목 3~4개로 짧게 끊어서.
6. **정확성 최우선**: 틀린 경제 정보 절대 금지. 불확실한 수치·고유명사는 피하고 일반 원리 위주로. 특정 종목 추천·투자 권유 금지.
7. **분량**: thin content 금지. 본문 800자 이상, 표나 리스트로 가독성 확보.
8. **본문 시각화 필수**: 글마다 내용 사이에 의미 있는 시각 자료를 **2~4개** 넣는다. 핵심 수치가 있으면 차트, 조건·항목 비교는 표, 흐름·원리는 도식, 장소·현장·실물 맥락은 출처가 분명한 사진을 우선한다. 장식용 이미지만 채우지 말고 각 자료 바로 아래에 `무엇을 보여주는지 + 기준일/단위 + 출처`를 적는다. 세부 제작 기준은 `WRITING_PLAYBOOK.md`의 **본문 시각화** 절을 따른다.
9. 글 끝에 **"핵심 정리"** 요약 섹션 + 다음 편 예고 한 줄.

## 작업 흐름 (자동)

1. `git checkout main && git pull origin main`
2. 새 글 1편과 해당 글의 시각 자료 작성 (`public/images/posts/<slug>/`)
3. 본문에 시각 자료 2~4개가 실제 삽입됐고 캡션·기준·출처가 있는지 확인
4. `npm run build` 로 빌드 통과 확인 (실패하면 고칠 것)
5. `auto/post-<slug>` 브랜치 생성 → 커밋 → push
6. `gh pr create` 로 PR 생성 → **새 글은 빌드 통과 시 바로 자동 머지** (`gh pr merge --squash --delete-branch`). 근거는 [`MERGE_POLICY.md`](./MERGE_POLICY.md) — **판단 기준은 그 파일에만 있고 여기선 가리키기만 한다.** 빌드가 실패하고 못 고치면 PR 열어둔 채 DM 보고.
   > ⚠️ 이 줄은 2026-08-31까지 `자동 머지 금지 (대표님이 리뷰)` 로 남아 `MERGE_POLICY.md` 와 **정반대**였다. 크론 지시문·정책 파일·이 문서 셋이 같은 규칙을 각각 들고 있으면 **하나는 반드시 낡는다.**
7. 생성된 PR URL을 출력
8. 적절한 새 주제가 없으면 글을 만들지 말고 `NO_NEW_POST` 를 출력하고 종료

## 후보 주제 풀 (참고 — 겹치지 않게 골라 쓰기)

물가/인플레이션, 환율, GDP, 금리와 채권, 주식 기초, ETF, 분산투자, 복리, 예금자보호, 신용점수, 가계부채, 부동산과 금리, 세금 기초(소득·부가세), 연금, 경제 사이클(호황·불황), 실업률, 무역수지, 기축통화, 가상자산 기초.
